%app-font-common-base {
  font-family: 'Roboto', 'Noto', sans-serif;
  -webkit-font-smoothing: antialiased;
}

%app-font-common-code {
  font-family: 'Roboto Mono', 'Consolas', 'Menlo', monospace;
  -webkit-font-smoothing: antialiased;
}

%app-font-common-nowrap {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

%app-font-display4 {
  @extend %app-font-common-base;
  @extend %app-font-common-nowrap;

  font-size: 112px;
  font-weight: 300;
  letter-spacing: -.044em;
  line-height: 120px;
}

%app-font-display3 {
  @extend %app-font-common-base;
  @extend %app-font-common-nowrap;

  font-size: 56px;
  font-weight: 400;
  letter-spacing: -.026em;
  line-height: 60px;
}

%app-font-display2 {
  @extend %app-font-common-base;

  font-size: 45px;
  font-weight: 400;
  letter-spacing: -.018em;
  line-height: 48px;
}

%app-font-display1 {
  @extend %app-font-common-base;

  font-size: 34px;
  font-weight: 400;
  letter-spacing: -.01em;
  line-height: 40px;
}

%app-font-headline {
  @extend %app-font-common-base;

  font-size: 24px;
  font-weight: 400;
  letter-spacing: -.012em;
  line-height: 32px;
}

%app-font-title {
  @extend %app-font-common-base;
  @extend %app-font-common-nowrap;

  font-size: 20px;
  font-weight: 500;
  line-height: 28px;
}

%app-font-subhead {
  @extend %app-font-common-base;

  font-size: 16px;
  font-weight: 400;
  line-height: 24px;
}

%app-font-body2 {
  @extend %app-font-common-base;

  font-size: 14px;
  font-weight: 500;
  line-height: 24px;
}

%app-font-body1 {
  @extend %app-font-common-base;

  font-size: 14px;
  font-weight: 400;
  line-height: 20px;
}

%app-font-caption {
  @extend %app-font-common-base;
  @extend %app-font-common-nowrap;

  font-size: 12px;
  font-weight: 400;
  letter-spacing: 0.011em;
  line-height: 20px;
}

%app-font-menu {
  @extend %app-font-common-base;
  @extend %app-font-common-nowrap;

  font-size: 13px;
  font-weight: 500;
  line-height: 24px;
}

%app-font-button {
  @extend %app-font-common-base;
  @extend %app-font-common-nowrap;

  font-size: 14px;
  font-weight: 500;
  letter-spacing: 0.018em;
  line-height: 24px;
  text-transform: uppercase;
}

%app-font-code2 {
  @extend %app-font-common-base;

  font-size: 14px;
  font-weight: 700;
  line-height: 20px;
}

%app-font-code1 {
  @extend %app-font-common-base;

  font-size: 14px;
  font-weight: 500;
  line-height: 20px;
}
